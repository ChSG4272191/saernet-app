import React from 'react';

function Button({nameButton1}) {

    
    return ( 
        <div>
        <nav>
            <button>{nameButton1}</button>
        </nav>
        </div>  
     );
}

export default Button;